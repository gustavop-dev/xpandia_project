---
description: Legacy alias for /debug — read-only diagnosis workflow for bugs, failing flows, or unexpected behavior
auto_execution_mode: 2
---

# Debugme (alias of `/debug`)

Use the same workflow as `/debug`:

- diagnose only
- do not modify files
- gather evidence first
- return observations, root cause, recommended fix, and verification steps

---

## Output final

Reportar siguiendo [[_output-protocol]]. Misma plantilla que `/debug`.
