interface:
  display_name: "Human"
  short_description: "Respuesta puntual y escaneable: conclusión primero..."
  default_prompt: "Use $human to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
