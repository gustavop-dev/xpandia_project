interface:
  display_name: "Frontend Unit Test Coverage"
  short_description: "Frontend unit coverage — cover untested behavior in stores..."
  default_prompt: "Use $frontend-unit-test-coverage to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
