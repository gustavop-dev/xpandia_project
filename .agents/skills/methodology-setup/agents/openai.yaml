interface:
  display_name: "Methodology Setup"
  short_description: "Initialize or refresh the Memory Bank methodology system..."
  default_prompt: "Use $methodology-setup to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
