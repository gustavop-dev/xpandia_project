interface:
  display_name: "Plan Task"
  short_description: "Wrapper parametrizado de `$plan` — recibe en `$ARGUMENTS` la..."
  default_prompt: "Use $plan-task to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
