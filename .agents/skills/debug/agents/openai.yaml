interface:
  display_name: "Debug"
  short_description: "Agentic debug mode — diagnose and analyze bugs without..."
  default_prompt: "Use $debug to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
