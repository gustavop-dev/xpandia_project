interface:
  display_name: "Deploy And Check"
  short_description: "VPS-only — Deploy a project (any environment). Defaults to..."
  default_prompt: "Use $deploy-and-check to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: false
