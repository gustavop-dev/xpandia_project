interface:
  display_name: "Backend Test Coverage"
  short_description: "Backend test coverage — cover untested behavior in models..."
  default_prompt: "Use $backend-test-coverage to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
