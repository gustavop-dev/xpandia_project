interface:
  display_name: "Migrate Project"
  short_description: "Migra un proyecto Django entre VPS del fleet (20 pasos: git..."
  default_prompt: "Use $migrate-project to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
