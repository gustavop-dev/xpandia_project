interface:
  display_name: "Implement"
  short_description: "Implementation workflow — systematic code protocol with..."
  default_prompt: "Use $implement to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
