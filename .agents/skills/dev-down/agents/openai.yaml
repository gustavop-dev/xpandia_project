interface:
  display_name: "Dev Down"
  short_description: "Detiene el entorno de desarrollo LOCAL levantado por..."
  default_prompt: "Use $dev-down to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: false
