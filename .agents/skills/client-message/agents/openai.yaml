interface:
  display_name: "Client Message"
  short_description: "Redacta el par correo + mensaje de WhatsApp para avisarle..."
  default_prompt: "Use $client-message to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
