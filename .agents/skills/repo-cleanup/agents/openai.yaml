interface:
  display_name: "Repo Cleanup"
  short_description: "Systematic audit of the repository to find obsolete..."
  default_prompt: "Use $repo-cleanup to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
