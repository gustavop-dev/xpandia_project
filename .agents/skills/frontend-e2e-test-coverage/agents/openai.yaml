interface:
  display_name: "Frontend E2e Test Coverage"
  short_description: "E2E coverage — close untested user flows with specs that..."
  default_prompt: "Use $frontend-e2e-test-coverage to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
