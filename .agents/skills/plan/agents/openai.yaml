interface:
  display_name: "Plan"
  short_description: "Planning workflow — read context, clarify requirements..."
  default_prompt: "Use $plan to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
